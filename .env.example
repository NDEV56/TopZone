# TopZone .env — copy jadi .env lalu isi
# Atau jalankan: node server.js (wizard otomatis muncul)

NGROK_AUTHTOKEN=

# auto | xampp | laragon | wamp | mamp | php | custom
SERVER_MODE=auto

# LOCAL_PORT=80
# PHP_PORT=8080
# PHP_ROOT=./Home
# NGROK_DOMAIN=
